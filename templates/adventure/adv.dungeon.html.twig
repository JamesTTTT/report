{% extends "adventure/adv.base.html.twig" %}
{% block title %}Jungle{% endblock %}

{% block body %}

{% set Diavis = 'block' %}
{% if diamond.getItem()[2] == true %}
    {% set Diavis = 'none' %}
{% endif %}
{# {{ dump(_context) }} #}
    <article class="content">
        <h1>Dungeon</h1>
        {# <button type="submit" name="addPickaxe" /> #}
        <div class="dungeon">
        {% set pick = 'block' %}
        <form method="post">

            <div class ="diamond4" style="display: {{Diavis}}">
                <div class ="item-container">
                <button type="submit" name="addDiamond" value="Diamond">
                <img src="../img/adventure/diamond.png" width="20" alt="diamond">
                </button>
            </div>
            </div>

            <div class ="back">
                <div class ="item-container">
                <button type="submit" name="back" value="Back">
                <img src="../img/adventure/back.png" width="70" alt="back">
                </button>
            </div>
            </div>
            </form>
        </div>

        <div class="info-log">
        {% for message in app.flashes('info') %}
        <div class="flash-notice">
            {{ message }}
        </div>
        {% endfor %}
        </div>

        <h3>Your Bag:</h3>
        <div class="bag">
        {% for item in bag %}
            <div class="bag-item">
                <img src="{{item.getItem()[1]}}" width="40">
                <p>{{item.getItem()[0]}}</p>
            </div>
        {% endfor %}
        </div>
    </article>
{% endblock %}